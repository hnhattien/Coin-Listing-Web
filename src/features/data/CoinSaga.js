import {takeLatest, put, take, call} from 'redux-saga/effects';
import API from '../../service/API/index';

import { RAPID_API_HOST, RAPID_COINGECKO_API_URL, RAPID_API_KEY } from '../../CoinmarketcapAPI/index';


function* requestMarketData(action) {
   
}

function* requestCoinNews24H() {
    try{
       
            

       
    }
    catch(err){
       console.log(err);
    }
}

export default function* watchCoinSaga(){
    
    
}